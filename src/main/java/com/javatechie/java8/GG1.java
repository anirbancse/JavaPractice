/*
package com.javatechie.java8;

import java.util.*;

public class GG1 {

    public static void main(String[] args) {
        int A[] = {1, 3, 2, 7, 9, 4, 5};
        Set list = new TreeSet<>();
        Integer.MAX_VALUE

        List list1 = new ArrayList();
        list1.stream().sorted().
        var x = Arrays.stream(A).sum();
        System.out.println(x);
        for (int i = 0; i < A.length; i++) {
            if (x <= A.length * A[i]) {
                list.add(A[i]);
            }
        }
        System.out.println(list);
    }
}
*/
